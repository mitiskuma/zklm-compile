# ZK-ML Proof Server — Python dependencies
#
# Derived from actual imports in serve.py and python/tvm_to_gkr/*.py
# CPU-only torch to keep the image small (no CUDA runtime).

--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
numpy>=1.24.0
transformers>=4.30.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
